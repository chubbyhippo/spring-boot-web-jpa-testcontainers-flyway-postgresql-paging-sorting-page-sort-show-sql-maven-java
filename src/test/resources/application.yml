spring:
  datasource:
    url: jdbc:tc:postgresql:latest:///
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        generate_statistics: true
  profiles:
    active: dev
logging:
  level:
    org:
      springframework: debug